[tool.uv.workspace]
members = ["trace_schema", "agent", "harness"]

[tool.pytest.ini_options]
testpaths = ["trace_schema/tests", "agent/tests"]
addopts = "--import-mode=importlib"
markers = ["integration: requires a running local Ollama"]
